import { ArticleLayout } from '@/components/ArticleLayout'
import Image from 'next/image'
import barcodeGenerator from './barcode-generator.png'

export const meta = {
  author: 'Bartas Urba',
  date: '2022-09-05',
  title: 'Writing a barcode generator program',
  description:
    'I wrote a custom barcode generator for a mobile device repair shop. I used the Python programming language for this project.',
}

export default (props) => <ArticleLayout meta={meta} {...props} />

I wrote a custom barcode generator for a mobile device repair shop. I used the [Python](https://www.python.org/) programming language and the [the Google API Python client](https://github.com/googleapis/google-api-python-client) for this project.

<Image src={barcodeGenerator} alt="" />

## App Features

- Generates PDF output
- Customized barcode renderer
- High performance code
