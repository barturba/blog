import { ArticleLayout } from '@/components/ArticleLayout'
import Image from 'next/image'

export const meta = {
  author: 'Bartas Urba',
  date: '2022-10-01',
  title: 'Creating a point of sale integration system',
  description: 'Shuttling data from legacy systems',
}

export default (props) => <ArticleLayout meta={meta} {...props} />

Shuttling data from legacy systems was the driver for this integration.

We created this system using [Python](https://www.python.org/).

## App Features

- Industry integrations
- Batch data pipeline
- National deployment
